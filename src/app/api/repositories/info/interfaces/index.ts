export interface Info {
    title: string;
    description: string;
    clientId: string;
}

export interface InfoRepositoryRepresentation {
    _id: string;
    title: string;
    description: string;
    clientId: string;
    createdAt: string;
    updatedAt: string;
}